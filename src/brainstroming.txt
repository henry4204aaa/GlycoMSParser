when -1arm  reduce 1H1N also

2arm 
F1H2N2S1
0.5 1 1 0.5

1arm
1 2 2 1 
-> 1 1 1 1

FHSN -> done 
FHN -> leave S -> ...?
->extension no
->go back to check other "terminal" epitopes
HNS -> leave F -> ...?
->extension no


after checking "all" terminal and nothing can be added anymore, arms -1

current version: for i in terminal (only runs once...)

假設現在arms是2
F 1 H 2 N 2 S 1
avg 0.5  1 1 0.5
減完arms:  0.5 0 0 0.5
真正的arms = 2
while armsno > 0:
	assignrealarm = arms  #給一個演算用的arm
	all_terminal_rejected = False #進迴圈用
	
	while not all_terminal_rejected: #當可以加terminal的時候(第一次一定進)
		all_terminal_rejected = True 
		#設定為True，除非等下for迴圈有找到否則直接退
		
		for i in terminal: #會跑一輪 terminal
			....
			#arms 2
			#計算用 = 0.5 0+1 0+1 0.5
			#arms 1
			#計算用 = 1 0+2 0+2 1
			加在同arm 		#用掉a 1 1 1 1  > 剩 0 1 1 0 -> 無解->進下一段
			第一次: arm 1 1111  第二次: arm2 剩11 測 剩下的arm數是否=LacNAc->歸零
			
			加在不同arm		#用掉b 1 1 1 0  > 剩 0 1 1 1 -> 用掉 0 1 1 1 -> 歸零
			第一次: arm1 1110  第二次 arm2 0111 歸零 不給跑第三次 assignrealarm
			其實跟上面一樣 	#用到c 0 1 1 1	> 剩 1 1 1 0 ->
			
			if i ok:
				add i into log
				all_terminal_rejected = False #改flag讓他能繼續跑
			else:
				continue (check next)
		assignrealarm
	assignrealarm = arms  #給一個演算用的arm
	all_terminal_rejected = False #進迴圈用

	while not all_extensive_rejected:
		從terminal跑完剩下的units繼續
		all_terminal_rejected = True
		for j in extensive: #會跑一輪 extensive
			....
			if i ok:
				add i into log
				all_terminal_rejected = False #改flag讓他能繼續跑
			else:
				continue (check next)
	add end message in log [n arms]
	armsno -1